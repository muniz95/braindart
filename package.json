{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webdev build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d ./build"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
